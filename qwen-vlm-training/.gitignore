# Generated by prepare_dataset.py — regenerate locally, don't commit.
train.json
val.json
dataset_info.json

# Python caches.
__pycache__/
*.pyc
.venv/

# Local training runs (write_train_config.py --output-dir).
runs/

# Staged upload folder (package_kaggle_dataset.py).
kaggle-upload/

# Downloaded Kaggle training-run archive (large; re-download as needed).
kaggle-trained/*.zip

# LoRA merge output from merge_lora.py (~7 GB HF model; regenerable).
merged/

# llama.cpp conversion outputs. Modelfile is tracked; GGUFs are not.
gguf/*.gguf

